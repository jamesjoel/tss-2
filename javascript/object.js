// An Object can store multipal values in a single variable, every value have own property (address)

//var a = {}; // empty object

var a = {x : 10, y : 20, z : 50};

a.name = "rohit";

console.log(a);

